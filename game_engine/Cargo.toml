[package]
name = "bevy_sample"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.19.0"

# WASM の配信サイズを抑えるための最適化。start が速くなり帯域も節約できる。
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
